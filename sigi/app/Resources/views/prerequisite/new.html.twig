{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Prerequisito</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary btn1" role="button" value="Crear" />
    {{ form_end(form) }}

    <a href="{{ path('prerequisite_index') }}" class="btn btn-info btn2" role="button">Lista de Prerequisitos</a>
    
{% endblock %}
